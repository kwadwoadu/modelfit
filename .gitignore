# Secrets and machine-specific config -- NEVER commit
.env
.env.*
!.env.example
config/models.json
*.local.sh
lib/keys.local.sh

# Run outputs (can contain large or sensitive model answers)
runs/

# Live results are per-user run data; ship results.example.csv instead
results.csv

# Demo render scratch (demo/demo.tape cleans these up; ignored in case a render is interrupted)
.demo-runs/
.demo-results.csv

# Junk
.DS_Store
*.tmp
*.log
