---
name: Bug report
about: Something in ModelFit did not work as expected
labels: bug
---

**What happened**
A clear description of the bug.

**To reproduce**
The exact `./bin/modelfit ...` command(s) and the relevant config (redact keys).

**Expected**
What you expected instead.

**Environment**
- OS:
- `jq --version`:
- Provider(s) / model(s) involved:

**Output**
Relevant terminal output, or the failing `runs/<id>/` files (redact anything sensitive).
